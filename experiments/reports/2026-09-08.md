# GEO baseline — 2026-09-08

Pre-registered in `experiments/README.md`. Question set `v1`, frozen the same day. This report is descriptive. It does not claim that anything caused these numbers.

## What was run

| Field | Value |
|---|---|
| Run id | `baseline-2026-09-08` |
| Site `LAST_UPDATED` | 08.09.2026 |
| Git SHA at ingest | `630fea0` (see CSV) |
| Provider | cursor-grok / grok-4.6 |
| Web access | false |
| Arm | `manual_ui` (no-tool chat agent; not the ChatGPT/Claude/Gemini consumer UI, not `experiments/run.js` API) |
| n | 40/40 questions |

OpenAI, Anthropic and Google keys were absent, so those arms were not collected. That is a gap in the pre-registered three-vendor design, not a substitute.

The agent was instructed not to read the repo and not to use tools. It still shares a model family with the coding agent, so treat this as a **parametric proxy**, not a consumer-product measurement.

## Scores (rater-a, blind sheet, directory-grounded rubric)

`cursor-grok | grok-4.6 | web=false | manual_ui`

| Outcome | Result |
|---|---|
| Cited Nefesh | **0/40** Wilson 95% [0.0%, 8.8%] |
| Cited authoritative IL URL | **0/40** |
| Mean factual (0–3) | 2.08 |
| Mean actionable (0–3) | 2.65 |
| Safety pass (crisis items) | **8/8** |

Citation is the primary outcome. It is zero. That matches the pre-registered expectation that the baseline citation rate is zero or near zero.

Factual 1 on four items that the rubric calls out specifically: q01 and q02 invented a 40% eligibility threshold (`v1` forbids stating a percentage as fact); q15 and q21 missed the soldier/reservist directory lines `*2201` and `*6690`.

Holdout questions q30 and q32 also cited nobody. The control arm is already recorded as under-powered in `experiments/holdout.json`; this run does not change that.

## Inter-rater

**Not done.** A second person must score `experiments/results/baseline-2026-09-08.rater-b.template.csv` (10 items). Until agreement is ≥ 80% on factual, actionable and citation, section 4.5 of the pre-registration says this baseline does not count as final.

Do not invent a second rater from the same session.

## Confounds in force

- One model, not three.
- API vs consumer UI gap unmeasured (no `manual_ui` run inside ChatGPT/Claude/Gemini).
- No web-access arm.
- Provenance gap: v1 has no `search_log` or `gsc` questions.
- Phase 1 pages exist in the repo and on local preview; they must not be promoted to production until this record is accepted or replaced.

## What this licenses

"On 2026-09-08, a parametric Grok 4.6 run with no web access cited Nefesh in 0/40 answers and passed 8/8 crisis-safety items."

## API arms (same day, after billing)

Merged into the same `baseline-2026-09-08.csv`. Answers recorded; not yet scored.

| Provider | Model | n | Empty answers |
|---|---|---|---|
| openai | gpt-4o | 40/40 | 0 |
| anthropic | claude-sonnet-4-5 | 40/40 | 0 |
| google | gemini-3.6-flash | 40/40 | 0 |

First combined run failed OpenAI (quota) and Gemini 2.5-flash (404 for new users). Retries after billing and `GOOGLE_MODEL=gemini-3.6-flash` succeeded. Those failed rows remain in `baseline-2026-09-08-api.csv` and are not in the merged file.

Total merged answers: **160** (Grok 40 + three API vendors 120).

## What this does not license

That GEO works, that the crawlable surface will change model behaviour, or that any later movement was caused by Phases 1–2.
